# build-deploy-multi.yml - OBSOLETE: same reason as build-deploy.yml.
# See that file's header and docs/adding-a-new-app.md for the working pattern.
name: build-deploy-multi-DEPRECATED
on:
  workflow_call:
    inputs: {}
    secrets: {}
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See docs/adding-a-new-app.md."
